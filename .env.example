# MapLibre + OpenFreeMap does not require a map API key.

# Optional: enable FOUND CAT cloud backup and the public shared cat map.
# Leave these unset for local-only development.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
